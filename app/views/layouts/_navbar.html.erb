<nav class="sticky top-0 z-50 w-full bg-green-950/40 backdrop-blur-md border-b border-green-500/30 shadow-[0_10px_50px_rgba(0,0,0,0.4)]">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      
      <div class="flex flex-1 items-center justify-start gap-8">
        <div class="flex shrink-0 items-center">
          <h1 class="font-archivo text-2xl font-bold tracking-tighter text-green-500 drop-shadow-[0_0_8px_rgba(34,197,94,0.6)]">
            KRYPTON
          </h1>
        </div>
        
        <div class="hidden md:block">
          <div class="flex space-x-1">
            <%= link_to "Home", root_path, class: "rounded-md px-3 py-2 text-xs font-bold text-green-500/70 hover:text-green-400 hover:bg-green-500/10 transition-all uppercase tracking-widest" %>
            <%= link_to "Posts", posts_path, class: "rounded-md px-3 py-2 text-xs font-bold text-green-500/70 hover:text-green-400 hover:bg-green-500/10 transition-all uppercase tracking-widest"%>
            <a href="#" class="rounded-md px-3 py-2 text-xs font-bold text-green-500/70 hover:text-green-400 hover:bg-green-500/10 transition-all uppercase tracking-widest">Library</a>
            <a href="#" class="rounded-md px-3 py-2 text-xs font-bold text-green-500/70 hover:text-green-400 hover:bg-green-500/10 transition-all uppercase tracking-widest">Friends</a>
          </div>
        </div>
      </div>

      <div class="flex items-center gap-4">
        <% if user_signed_in? %>
          <div class="flex items-center gap-4">
            <div class="flex flex-col items-end">
              <span class="text-[10px] text-green-700 font-bold uppercase tracking-tighter">Authorized Access</span>
              <span class="text-xs font-archivo text-green-200">
                <%= current_user.email.split('@').first.upcase %>
              </span>
            </div>
            
            <%= button_to destroy_user_session_path, method: :delete, 
                class: "rounded border border-red-500/40 bg-red-500/10 px-3 py-1.5 text-[10px] font-archivo font-bold text-red-500 hover:bg-red-600 hover:text-white transition-all cursor-pointer uppercase shadow-[0_0_10px_rgba(239,68,68,0.2)]" do %>
              Terminate
            <% end %>
          </div>

        <% else %>
          <%= link_to new_user_session_path, 
              class: "rounded-lg bg-green-600 px-6 py-2.5 text-xs font-archivo font-bold text-gray-950 hover:bg-green-400 shadow-[0_0_20px_rgba(34,197,94,0.4)] transition-all uppercase tracking-tighter" do %>
            Login Protocol
          <% end %>
        <% end %>
      </div>

    </div>
  </div>

  <div class="md:hidden border-t border-green-500/10 bg-black/20">
    <div class="flex justify-around py-2">
      <a href="#" class="text-[10px] font-bold text-green-500/60 uppercase">Posts</a>
      <a href="#" class="text-[10px] font-bold text-green-500/60 uppercase">Library</a>
      <a href="#" class="text-[10px] font-bold text-green-500/60 uppercase">Friends</a>
    </div>
  </div>
</nav>
